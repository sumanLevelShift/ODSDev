import { LightningElement,api,track } from 'lwc';

export default class CompChild extends LightningElement {
    /*
    @track upperCaseItem ;

    @api 
    changeToUpperCase(text){
        this.upperCaseItem = text.toUpperCase();
    }*/
}